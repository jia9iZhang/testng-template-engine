package com.atta.test.dal.dao;

import com.atta.test.common.TestConstants;
import com.atta.test.util.DBUtils;

import java.util.Map;

/**
* @Description TODO
* @Author ${authorName}
* @Date ${date}
*/
public class ${daoName}{

    // 需要根据自己的需要选择目标数据库实例
    // String DB_INSTANCE = TestConstants.DATABASE_INSTANCE_CALLCENTER;
    public static final String DB_INSTANCE = "";

    /**
    * 通过 id 查询
    * @param id
    */
    public static String getByID(String id) {
        String sql = ""+id;
        return DBUtils.queryFirstRecord(DB_INSTANCE,sql);
    }
}
